#!/usr/bin/env python
from setuptools import setup

setup(name='dh_segment',
      version='0.1',
      url='https://github.com/dhlab-epfl/dhSegment',
      packages=['dh_segment'],
      zip_safe=False)